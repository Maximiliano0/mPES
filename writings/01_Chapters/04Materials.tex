\documentclass[../00_Main/Main.tex]{subfiles}
\begin{document}

\label{sec:methods}

Este cap\'itulo describe el aparato experimental: el repositorio
\texttt{mPES}, la definici\'on formal del entorno, los seis paquetes
algor\'itmicos comparados, el pipeline de optimizaci\'on bayesiana de
hiperpar\'ametros y el protocolo de evaluaci\'on.

\subsection{Repositorio y reproducibilidad}

Todos los experimentos se ejecutan dentro del \emph{workspace}
\texttt{mPES} (\url{https://github.com/Maximiliano0/mPES_2026}), un
proyecto Python organizado en dos familias: \texttt{tabular/}
(m\'etodos enumerativos) y \texttt{ml/} (aprendizaje profundo). Cada
paquete es autocontenido: provee su propio entorno
\texttt{gymnasium}~\citep{Brockman2016,Towers2024}, su rutina de
entrenamiento, su m\'odulo de optimizaci\'on bayesiana y su carpeta
\texttt{outputs/} versionada por fecha. Las dependencias principales
son TensorFlow 2.21, Keras 3.13, NumPy 2.4, Optuna 4.7,
Gymnasium 1.2 y SciPy 1.17.

\subsection{El entorno: \emph{Pandemic Scenario}}

\subsubsection{Definici\'on formal}

El entorno modela una pandemia simplificada como un MDP finito en el
que el agente decide, en cada \emph{trial}, cu\'antas unidades de un
recurso escaso destinar a contener un brote. El espacio de estados
es

\begin{equation}
\mathcal{S} \;=\;
\{(R, t, S) : R \in \{0, \ldots, 39\},\ t \in \{1, \ldots, T\},\
S \in \{0, \ldots, S_{\max}\}\},
\end{equation}

con $R$ los recursos restantes, $t$ el \'indice de \emph{trial} y $S$
la severidad actual; $|\mathcal{S}| = 3\,410$ estados alcanzables.
El espacio de acciones es $\mathcal{A} = \{0, 1, \ldots, 10\}$
(asignaciones que exceden $R$ son enmascaradas).

\subsubsection{Din\'amica y recompensa}

La din\'amica de la severidad sigue la ley exponencial-corregida

\begin{equation}
S_{t+1} \;=\; \max\!\bigl(0,\ \beta \cdot S_{t} - \alpha \cdot a_{t}\bigr),
\qquad \alpha = 0{,}4,\ \beta = 1{,}4,
\label{eq:transicion}
\end{equation}

con $\beta > 1$ capturando el crecimiento natural del contagio
(inercia) y $\alpha$ la eficacia marginal de cada unidad asignada. La
recompensa instant\'anea es

\begin{equation}
r_t \;=\; -\,S_{t},
\end{equation}

de modo que la pol\'itica \'optima minimiza la severidad
acumulada total, en analog\'ia directa con la mortalidad agregada de
la pandemia simulada.

\subsubsection{Estructura del benchmark}

Cada experimento del repositorio \texttt{mPES} se organiza
jer\'arquicamente en \textbf{8 bloques} de \textbf{8 secuencias}
de \textbf{3--10 \emph{trials}} cada una, totalizando
$\approx 360$ \emph{trials} por ejecuci\'on. La evaluaci\'on
final se realiza sobre $n = 64$ secuencias fijas
($8 \times 8$, semilla $= 42$), garantizando que los ocho modelos
enfrenten el \emph{mismo} conjunto \emph{out-of-distribution}. El
cat\'alogo incluye \textbf{22 escenarios} agrupados en cuatro
familias --- \emph{severidad} (10), \emph{longitud} (5),
\emph{conjunta} (4) y \emph{estructural} (3) --- de modo que el
barrido completo del orquestador
(\texttt{general/scripts/orchestrate.py}) abarca
$7 \times 22 = 154$ celdas (todos los paquetes excepto
\texttt{pes\_base}, que act\'ua como cota inferior de referencia y
no requiere optimizaci\'on). La Figura~\ref{fig:per-sequence}
muestra ejemplos representativos de la distribuci\'on emp\'irica
de severidad junto a tres familias OOD utilizadas para estresar la
pol\'itica aprendida: extrapolaci\'on a severidades altas,
escenarios estructurales con mayor n\'umero total de \emph{trials}
y combinaciones extremas conjuntas.

\begin{figure}[h!]
  \centering
  \includegraphics[width=\linewidth]{per_sequence_4panel.pdf}
  \caption{Desempe\~no por secuencia para cuatro familias OOD del
  benchmark ($n = 64$ secuencias por panel, ordenadas de menor a
  mayor retorno). El \emph{ensemble} \texttt{pes\_ens} se traza en
  negro y con trazo m\'as grueso para destacar su rol de cota
  superior frente a los siete paquetes individuales.}
  \label{fig:per-sequence}
\end{figure}

\subsection{Los ocho paquetes algor\'itmicos}

La Tabla~\ref{tab:packages} resume las ocho arquitecturas
evaluadas. El repositorio \texttt{mPES} organiza estas variantes
en dos familias: \emph{tabular} (m\'etodos enumerativos con
representaci\'on exacta del MDP) y \emph{profunda} (redes neuronales
entrenadas por descenso de gradiente). Cada paquete comparte el
entorno Gymnasium descrito en la
Ecuaci\'on~\eqref{eq:transicion}, el bucle externo de optimizaci\'on
bayesiana con Optuna y el mismo conjunto fijo de $64$ secuencias
de validaci\'on, garantizando una comparaci\'on rigurosa
\emph{ceteris paribus}.

\begin{table}[h!]
\centering
\caption{Las ocho arquitecturas evaluadas en el \emph{workspace}
\texttt{mPES}. Las dos \'ultimas filas corresponden a las
extensiones recurrente y de \emph{ensemble} integradas en el
benchmark final.}
\label{tab:packages}
\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Paquete} & \textbf{Familia} & \textbf{Caracter\'istica distintiva} \\
\midrule
\texttt{pes\_base} & Tabular & Q-Learning de referencia~\citep{Watkins1992}.\\
\texttt{pes\_ql}   & Tabular & Q-Learning + Optuna/TPE.\\
\texttt{pes\_dql}  & Tabular & Double Q-Learning + PBRS + $\varepsilon$ \emph{warm-up}.\\
\texttt{pes\_dqn}  & Profundo & DQN con \emph{replay} y red objetivo~\citep{Mnih2015}.\\
\texttt{pes\_rdqn} & Profundo & Recurrent DQN con codificador LSTM~\citep{Hausknecht2015,Hochreiter1997}.\\
\texttt{pes\_a2c}  & Profundo & Actor--Cr\'itico (A2C)~\citep{Mnih2016}.\\
\texttt{pes\_trf}  & Profundo & Codificador Transformer causal sobre historia~\citep{Vaswani2017}.\\
\texttt{pes\_ens}  & Profundo & \emph{Soft voting} de DQN+RDQN+TRF~\citep{Lakshminarayanan2017}.\\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Familia tabular}

\paragraph{\texttt{pes\_base}.} Q-Learning enumerativo con
hiperpar\'ametros fijos; sirve de l\'inea de base inferior.

\paragraph{\texttt{pes\_ql}.} A\~nade un bucle externo Optuna/TPE que
muestrea $\alpha,\gamma$ y la pol\'itica de $\varepsilon$.

\paragraph{\texttt{pes\_dql}.} Implementa Double
Q-Learning~\citep{Hasselt2010}, una fase de calentamiento de
$\varepsilon$ y \emph{Potential-Based Reward Shaping}
$\Phi(s) = -S$~\citep{Ng1999}, especialmente \'util para acelerar la
convergencia ante una se\~nal de recompensa concentrada al final del
\emph{trial}.

\subsubsection{Familia profunda}

\paragraph{\texttt{pes\_dqn}.} Red densa Multi-Layer Perceptron con
buffer de repetici\'on de experiencia~\citep{Lin1992} y red objetivo
sincronizada cada $C$ pasos (Ecuaci\'on~\ref{eq:dqn-loss}). La
arquitectura concreta es
\texttt{Input}$(3) \!\to\! \texttt{Dense}(64,\!\mathrm{ReLU}) \!\to\!
\texttt{Dense}(64,\!\mathrm{ReLU}) \!\to\! \texttt{Dense}(11)$,
operando sobre el vector normalizado
$s = [r/30,\, t/10,\, \sigma/9]$. La interpolaci\'on entre estados
contiguos elimina la maldici\'on de la dimensionalidad que aquejaba a
la familia tabular.

\paragraph{\texttt{pes\_rdqn}.} Variante recurrente que incorpora
una capa \emph{Long Short-Term Memory}~\citep{Hochreiter1997} entre
la entrada normalizada y el cabezal de valor. Sigue la receta del
\emph{Deep Recurrent Q-Network}
(DRQN)~\citep{Hausknecht2015}: la red recibe una ventana hist\'orica
$(s_{t-W+1}, \ldots, s_t)$, propaga el estado oculto $h_t$ a trav\'es
de la celda LSTM (Ec.~\ref{eq:lstm}) y produce $Q(h_t, \cdot)$. Esto
le permite recuperar parte de la informaci\'on perdida cuando la
representaci\'on instant\'anea $s_t$ no es markoviana --- en
particular, la \emph{aceleraci\'on} del contagio que requiere
comparar al menos dos pasos consecutivos de severidad.

\paragraph{\texttt{pes\_a2c}.} Dos cabezales independientes (actor y
cr\'itico) sobre un tronco compartido; entrenamiento s\'incrono
n-\emph{step} con estimaci\'on de ventaja
(Ecuaci\'on~\ref{eq:pg})~\citep{Mnih2016}. El gradiente sigue el
teorema de pol\'itica de Sutton \& Barto~\citep{SuttonBarto2018}:
$\nabla_\theta J(\theta) =
\mathbb{E}_{\pi_\theta}\!\left[\nabla_\theta
\log \pi_\theta(a\mid s)\, A^{\pi}(s,a)\right]$,
con $A^{\pi}(s,a) = Q^{\pi}(s,a) - V^{\pi}(s)$ estimada por el
cr\'itico para reducir la varianza del estimador Monte Carlo.

\paragraph{\texttt{pes\_trf}.} Codificador Transformer causal de
$L$ capas, $h$ cabezas y dimensi\'on $d_{\mathrm{model}}$. La entrada
es la \emph{historia} de los \'ultimos $W$ tuplas
$(s_{t-W+1}, a_{t-W+1}, r_{t-W+1}, \ldots, s_t)$; la salida sobre la
\'ultima posici\'on alimenta una cabeza lineal que produce los
$|\mathcal{A}|$ \emph{logits}. La m\'ascara causal
(Ecuaci\'on~\ref{eq:causal-attention}) impide cualquier fuga
temporal. El n\'ucleo computacional es la \emph{scaled dot-product
attention} de Vaswani et al.~\citep{Vaswani2017}:
$\mathrm{Attention}(Q, K, V) = \mathrm{softmax}\!\bigl(
QK^{\top}/\sqrt{d_k}\bigr) V$, distribuida en $h$ cabezas paralelas
para atender simult\'aneamente a m\'ultiples horizontes
temporales. La distribuci\'on \texttt{softmax} sobre los \emph{logits}
es la base del c\'alculo de la \textbf{entrop\'ia de Shannon}
(Ecuaci\'on~\ref{eq:shannon}) usada como m\'etrica de UQ.

\paragraph{\texttt{pes\_ens}.} \emph{Soft voting} sobre las tres
arquitecturas profundas con mejores trazas individuales
($\{\texttt{pes\_dqn}, \texttt{pes\_rdqn}, \texttt{pes\_trf}\}$). En
tiempo de inferencia cada miembro $k$ aporta sus \emph{logits}
$\ell^{(k)}(s)$; el ensemble emite
\begin{equation}
  \pi_{\mathrm{ens}}(a \mid s) \;=\;
  \mathrm{softmax}\!\Bigl(
    \sum_{k=1}^{K} w_k \, \ell^{(k)}(s)
  \Bigr),
  \qquad \sum_k w_k = 1,
  \label{eq:ens-soft}
\end{equation}
con pesos $w_k$ proporcionales al desempe\~no \emph{in-distribution}
de cada miembro. El paquete no introduce par\'ametros entrenables
nuevos: se limita a cargar los modelos \texttt{.keras} producidos
por sus hermanos y promediar sus distribuciones, en l\'inea con la
formulaci\'on cl\'asica de \emph{deep ensembles} para
incertidumbre predictiva~\citep{Lakshminarayanan2017,Dietterich2000}.
La reducci\'on de varianza es estructural: si cada miembro tiene
error $\sigma_k^2$ y correlaci\'on $\rho$,
$\sigma_{\mathrm{ens}}^2 = \bar{\sigma}^2 \bigl(\rho +
(1-\rho)/K\bigr) \le \bar{\sigma}^2$.

\subsection{Optimizaci\'on Bayesiana de Hiperpar\'ametros}

Cada paquete profundo expone un m\'odulo \texttt{optimize\_*.py} que
ejecuta un estudio Optuna con estimador estructurado en \'arbol de
Parzen (TPE)~\citep{Bergstra2011,Snoek2012,Akiba2019}. La funci\'on
objetivo $f(\boldsymbol{\theta})$ es la mediana del retorno
descontado sobre las $n = 64$ secuencias de validaci\'on. Los
estudios incluyen \emph{pruning} mediante \texttt{MedianPruner}
para descartar tempranamente configuraciones inviables y conservan
el almacenamiento SQLite (\texttt{optuna\_storage.db}) en
\texttt{outputs/}, permitiendo reanudaci\'on y visualizaci\'on con
\texttt{optuna-dashboard}.

La Figura~\ref{fig:optuna-trf} ilustra el progreso de la b\'usqueda
TPE para \texttt{pes\_trf} y la importancia relativa de cada
hiperpar\'ametro estimada por an\'alisis funcional de varianza
(fANOVA). El historial muestra la curva caracter\'istica de
mejora asint\'otica del estimador, mientras que el panel de
importancias permite focalizar futuros estudios sobre los
hiperpar\'ametros cr\'iticos.

\begin{figure}[h!]
  \centering
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{optimization_history.png}
    \caption{Historial de \emph{trials} Optuna/TPE.}
    \label{fig:optuna-history}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{hyperparameter_importances.png}
    \caption{Importancia relativa (fANOVA).}
    \label{fig:optuna-importance}
  \end{subfigure}
  \caption{Optimizaci\'on bayesiana de hiperpar\'ametros para
  \texttt{pes\_trf}: progreso de la b\'usqueda e importancia relativa
  de cada hiperpar\'ametro.}
  \label{fig:optuna-trf}
\end{figure}

La Figura~\ref{fig:training-process} compara las curvas de
recompensa acumulada por episodio durante el entrenamiento de
cuatro de los modelos profundos/tabular. Estas curvas resumen la
din\'amica de aprendizaje y permiten contrastar la velocidad de
convergencia entre familias antes del despliegue sobre el conjunto
de evaluaci\'on OOD.

\begin{figure}[h!]
  \centering
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{dql_rewards_vs_episodes.png}
    \caption{\texttt{pes\_dql} (Double Q-Learning + PBRS).}
    \label{fig:proc-dql}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{dqn_rewards_vs_episodes.png}
    \caption{\texttt{pes\_dqn} (DQN con \emph{replay}).}
    \label{fig:proc-dqn}
  \end{subfigure}
  \\[0.4em]
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{a2c_rewards_vs_episodes.png}
    \caption{\texttt{pes\_a2c} (Actor--Cr\'itico).}
    \label{fig:proc-a2c}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{trf_rewards_vs_episodes.png}
    \caption{\texttt{pes\_trf} (Transformer causal).}
    \label{fig:proc-trf}
  \end{subfigure}
  \caption{Curvas de recompensa acumulada por episodio durante el
  entrenamiento de los cuatro modelos profundos y tabular
  optimizados. Cada panel se obtuvo con los hiperpar\'ametros
  resultantes del estudio Optuna correspondiente.}
  \label{fig:training-process}
\end{figure}

\subsection{Calibraci\'on con un agente aleatorio}\label{sec:baseline-random}

Antes de comparar arquitecturas conviene fijar el \emph{piso de
desempe\~no}. La Figura~\ref{fig:baseline-random} muestra el comportamiento
de un agente puramente aleatorio --- una pol\'itica uniforme sobre
$\mathcal{A}$ --- evaluado sobre el mismo \emph{benchmark} de $64$
secuencias. Su desempe\~no normalizado se sit\'ua cerca de
$0{,}5$, lejos de cualquier modelo entrenado pero por encima de la
pol\'itica trivialmente cero (no asignar nunca, $\bar{r} \approx 0{,}30$).
Esta calibraci\'on confirma que el \emph{benchmark} discrimina,
y permite leer las m\'etricas de los modelos como \emph{ganancia
sobre el azar}.

\begin{figure}[h!]
  \centering
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{random_player_sequence_performance.png}
    \caption{Desempe\~no por secuencia.}
    \label{fig:rand-seq}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{random_player_normalised_performance.png}
    \caption{Desempe\~no normalizado acumulado.}
    \label{fig:rand-norm}
  \end{subfigure}
  \caption{Agente aleatorio de referencia evaluado sobre las
  $64$ secuencias de validaci\'on. Sirve como cota inferior para
  interpretar los resultados de los modelos entrenados.}
  \label{fig:baseline-random}
\end{figure}

\subsection{Trazas internas del agente Transformer}\label{sec:agent-internals}

La Figura~\ref{fig:trf-internals} expone variables internas
registradas durante el entrenamiento de \texttt{pes\_trf}: la
confianza por acci\'on derivada de la salida \emph{softmax} y su
versi\'on remapeada al espacio de severidad. Estas trazas son las
mismas que en el Cap.~\ref{sec:discussion} se utilizar\'an para
ligar la entrop\'ia de Shannon con la noci\'on neurocognitiva de
confianza~\citep{Boldt2015,Fleming2024}.

\begin{figure}[h!]
  \centering
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{trf_agent_confidences.png}
    \caption{Confianza \emph{softmax} por acci\'on.}
    \label{fig:trf-conf}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{trf_agent_remapped_confidences.png}
    \caption{Confianzas remapeadas al espacio de severidad.}
    \label{fig:trf-conf-remap}
  \end{subfigure}
  \\[0.4em]
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{trf_agent_cumulative_performance.png}
    \caption{Desempe\~no acumulado.}
    \label{fig:trf-cum}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\textwidth}
    \centering
    \includegraphics[width=\linewidth]{trf_agent_normalised_performance.png}
    \caption{Desempe\~no normalizado.}
    \label{fig:trf-norm}
  \end{subfigure}
  \caption{Trazas internas del agente \texttt{pes\_trf} durante el
  entrenamiento. Los paneles superiores ilustran la se\~nal de
  confianza utilizada en el Cap.~\ref{sec:discussion}.}
  \label{fig:trf-internals}
\end{figure}

\subsection{Protocolo de entrenamiento y evaluaci\'on}

\begin{enumerate}
\item \textbf{B\'usqueda Bayesiana.} 100--300 \emph{trials} Optuna por
paquete (las redes profundas requieren m\'as \emph{trials} por su
mayor dimensi\'on de hiperpar\'ametros).
\item \textbf{Reentrenamiento final.} El mejor conjunto
$\boldsymbol{\theta}^{*}$ se reentrena desde cero por
$E_{\max} = 2000$ episodios.
\item \textbf{Evaluaci\'on \emph{out-of-policy}.} Se evaluan las
$n = 64$ secuencias de validaci\'on con la pol\'itica
$\varepsilon$-greedy ($\varepsilon = 0$).
\item \textbf{An\'alisis estad\'istico.} Para cada par de paquetes
$(A, B)$ se reporta la media, desv\'iaci\'on est\'andar, $d$ de
Cohen~\citep{Cohen1988}, $t$/$p$ de Welch~\citep{Welch1947} y
divergencia KL entre los histogramas de acci\'on, agregadas como
matrices y \emph{heatmaps} (Cap.~\ref{sec:results}).
\end{enumerate}

\subsection{Generaci\'on autom\'atica del reporte}

El orquestador \texttt{general/scripts/orchestrate.py} ejecuta todos
los paquetes, agrega resultados con \texttt{aggregate.py}, produce
los \emph{heatmaps} con \texttt{plot\_matrix.py} y emite el reporte
final \texttt{benchmark\_report.md} mediante \texttt{report.py}. Esto
garantiza que cada cifra reportada en el Cap.~\ref{sec:results}
provenga de un pipeline reproducible y versionado.

\biblio % Needed for referencing to working when compiling individual subfiles - Do not remove
\end{document}

